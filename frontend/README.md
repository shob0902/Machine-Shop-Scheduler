# Frontend

This is the React + TypeScript + Vite + Tailwind frontend for
**The Machine Shop Scheduler**. See the [project README](../README.md) at
the repository root for the full setup, architecture, and deployment
instructions.

Quick start:

```bash
npm install
cp .env.example .env.local   # set VITE_API_BASE_URL
npm run dev
```
